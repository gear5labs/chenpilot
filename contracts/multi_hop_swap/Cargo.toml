[package]
name = "multi_hop_swap"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "22.0.0"
contract_failure = { path = "../contract_failure" }

[dev-dependencies]
soroban-sdk = { version = "22.0.0", features = ["testutils"] }
diff_fuzz_engine = { path = "../diff_fuzz_engine" }
